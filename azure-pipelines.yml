# Docker
# Build and push an image to Azure Container Registry
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

# trigger the pipeline when creating a tag with -rc* suffix
trigger:
  tags:
    include:
      - '*.*.*-rc*'

resources:
- repo: self

variables:
  # Container registry service connection established during pipeline creation
  dockerRegistryServiceConnection: '8552ded1-0fd3-4204-9481-ab0101c1ead2'
  imageRepository: 'hfpanalytics/api'
  containerRegistry: 'hfpanalyticsregistry.azurecr.io'
  dockerfilePath: '$(Build.SourcesDirectory)/Dockerfile.api_deploy'
  versionTag: '$(Build.SourceBranchName)'

  # Agent VM image name
  vmImageName: 'ubuntu-latest'

stages:
- stage: Build
  displayName: Build and push test
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: Docker@2
      displayName: Build and push an image to container registry
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        containerRegistry: $(dockerRegistryServiceConnection)
        tags: |
          $(versionTag)
